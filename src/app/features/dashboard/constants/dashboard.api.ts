import { environment } from '../../../../environments/environment';

export const DASHBOARD_API = {
  ADMIN: `${environment.apiUrl}/dashboard/admin`,
  STAFF: `${environment.apiUrl}/dashboard/staff`,
};
